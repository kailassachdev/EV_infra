import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function NewStation() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>New Station</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the New Station tab. Add new station functionality here.</p>
      </CardContent>
    </Card>
  );
} 